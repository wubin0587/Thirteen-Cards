name: cards_thirteen
description: 十三水 — 本地双人至十二人本地理牌
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  audioplayers: ^6.6.0
  ffi: ^2.0.0
  flutter_tts: ^4.2.5
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/sounds/
    - assets/models/thirteen_ranker.onnx
